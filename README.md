# Курсовой проект по DRF (Django REST Framework)

[![Deploy to Server](https://github.com/Rezilek/kurs_project_11_course_new/actions/workflows/deploy.yml/badge.svg)](https://github.com/Rezilek/kurs_project_11_course_new/actions/workflows/deploy.yml)

## 📋 Описание проекта

Проект представляет собой платформу для онлайн-курсов с реализованным API. В проекте реализованы:
- Управление курсами, уроками и подписками
- Платежная система через Stripe
- Фоновые задачи через Celery
- Кеширование через Redis
- Аутентификация и авторизация пользователей
- Разграничение прав доступа (модераторы, контент-менеджеры, пользователи)

## 🌐 Адрес развернутого приложения

**Сервер в Yandex Cloud:**
- Главная страница: http://178.154.198.79:8000
- Админ-панель: http://178.154.198.79:8000/admin

**Учетные данные для админки:**
- Логин: `admin`
- Пароль: `admin123`

## 🏗️ Архитектура проекта

Проект состоит из 6 Docker-контейнеров:
- **web** - Django приложение + Gunicorn
- **db** - PostgreSQL база данных
- **redis** - Redis для кеширования и брокера сообщений
- **celery** - Celery worker для фоновых задач
- **celery-beat** - Celery beat для периодических задач
- **nginx** - Nginx для раздачи статики и проксирования

## 🚀 Локальный запуск проекта (Windows 11)

### Предварительные требования
- Установленный [Docker Desktop](https://www.docker.com/products/docker-desktop/)
- Установленный [Git](https://git-scm.com/download/win)
- PowerShell (встроен в Windows)

### Пошаговая инструкция

1. **Клонируйте репозиторий:**
   ```powershell
   git clone https://github.com/Rezilek/kurs_project_11_course_new.git
   cd kurs_project_11_course_new
Создайте файл с переменными окружения:

powershell
cp .env.example .env
Отредактируйте файл .env, укажите реальные значения:

env
SECRET_KEY=your-secret-key-here
DEBUG=1
ALLOWED_HOSTS=localhost 127.0.0.1

POSTGRES_DB=course_db
POSTGRES_USER=course_user
POSTGRES_PASSWORD=course_password
Запустите проект с помощью Docker Compose:

powershell
docker compose up -d --build
Примените миграции и создайте суперпользователя:

powershell
docker compose exec web python manage.py migrate
docker compose exec web python manage.py createsuperuser
Откройте приложение в браузере:

http://localhost:8000

http://localhost:8000/admin

Остановка проекта:

powershell
docker compose down
Полезные команды
powershell
# Просмотр логов
docker compose logs -f

# Просмотр запущенных контейнеров
docker compose ps

# Перезапуск контейнеров
docker compose restart

# Выполнение команд внутри контейнера
docker compose exec web python manage.py shell
🔧 Настройка CI/CD и автоматического деплоя
Архитектура CI/CD
Проект использует GitHub Actions для автоматического тестирования и деплоя:

Push в ветку main → запуск CI/CD пайплайна

Тестирование - flake8 и тесты Django

Деплой - автоматическое развертывание на сервере Yandex Cloud

Настройка сервера (Yandex Cloud)
Создание виртуальной машины:

Платформа: Ubuntu 22.04 LTS

vCPU: 2, RAM: 2-4 GB

Диск: SSD 20-30 GB

Публичный IP: обязательно

Установка Docker и Docker Compose на сервере:

bash
sudo apt update
sudo apt install -y docker.io docker-compose-plugin
sudo usermod -aG docker $USER
Настройка SSH-доступа:

powershell
# На локальном компьютере создайте SSH-ключ
ssh-keygen -t ed25519 -C "your_email@example.com"

# Скопируйте публичный ключ на сервер
ssh-copy-id yc-user@178.154.198.79
Настройка GitHub Secrets
Для работы автоматического деплоя необходимо добавить следующие секреты в репозиторий GitHub:
Settings → Secrets and variables → Actions

Secret Name	Описание	Значение
SSH_HOST	IP-адрес сервера	178.154.198.79
SSH_USER	Пользователь для SSH	yc-user
SSH_PORT	Порт SSH	22
SSH_PRIVATE_KEY	Приватный SSH ключ	Содержимое файла id_ed25519
Процесс деплоя
При каждом пуше в ветку main автоматически:

Запускаются тесты и линтинг кода

Проверяется возможность сборки Docker-образов

При успешных проверках код копируется на сервер

Перезапускаются Docker-контейнеры с новой версией

📁 Структура проекта
text
kurs_project_11_course_new/
├── .github/
│   └── workflows/
│       └── deploy.yml          # Конфигурация GitHub Actions
├── config/                      # Настройки Django проекта
├── courses/                     # Приложение для курсов
├── users/                       # Приложение для пользователей
├── nginx/
│   └── nginx.conf               # Конфигурация Nginx
├── .env.example                  # Шаблон переменных окружения
├── .flake8                       # Конфигурация flake8
├── .gitignore                    # Игнорируемые файлы
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile                    # Dockerfile для Django
├── manage.py                      # Утилита Django
└── requirements.txt              # Зависимости Python
🛠️ Технологии
Backend: Django 4.x, Django REST Framework

База данных: PostgreSQL 15

Кеш и брокер: Redis 7

Фоновые задачи: Celery

Веб-сервер: Nginx + Gunicorn

Контейнеризация: Docker, Docker Compose

CI/CD: GitHub Actions

Облачная платформа: Yandex Cloud

🔒 Безопасность
Все секреты хранятся в переменных окружения (файл .env не попадает в git)

Приватные SSH-ключи хранятся в GitHub Secrets

Используется отдельный пользователь для сервера

Отключен DEBUG режим на продакшене

📝 Лицензия
Проект выполнен в рамках учебного курса.

👨‍💻 Автор
Студент курса Python-разработки

🆘 Поддержка
При возникновении проблем:

Проверьте логи: docker compose logs -f

Перезапустите контейнеры: docker compose restart

Создайте Issue в репозитории GitHub
# 🎓 Образовательная платформа на Django DRF

Проект представляет собой REST API для образовательной платформы с системой управления курсами, уроками и пользователями.

## 🚀 Быстрый старт

### Требования
- Python 3.13+
- PostgreSQL 13+
- Git

### Установка

## 1. Клонирование репозитория
git clone https://github.com/Rezilek/Kurs_project_11_course.git
cd Kurs_project_11_course

## 2. Создание виртуального окружения
python -m venv venv

## 3. Активация окружения

#### Windows:
venv\Scripts\activate
#### Linux/Mac:
source venv/bin/activate

## 4. Установка зависимостей
pip install -r requirements.txt

## 5. Настройка базы данных
#### Создайте файл .env из .env.example и настройте подключение к PostgreSQL

## 6. Применение миграций
python manage.py migrate

## 7. Создание суперпользователя
python manage.py createsuperuser

## 8. Запуск сервера
python manage.py runserver

## 📚 Документация API
Полная документация доступна в DEMO.md

### Основные эндпоинты:
GET /api/users/test-encoding/ - тест кодировки UTF-8
POST /api/users/token/ - получение JWT токена
GET /api/users/users/me/ - профиль текущего пользователя
GET /api/courses/courses/ - список курсов
GET /api/users/payments/ - платежи с фильтрацией

### 🧪 Тестирование
Запустите тестовый скрипт для проверки всех функций:

### python test_final.py
Или используйте Postman для ручного тестирования.

### 🏗️ Архитектура

#### Технологический стек:
Backend: Django 4.2 + Django REST Framework
База данных: PostgreSQL с UTF-8 кодировкой
Аутентификация: JWT (Simple JWT)
Кодировка: UTF-8 с поддержкой кириллицы

#### Ключевые особенности:
Кастомная модель User с авторизацией по email
JWT аутентификация с refresh токенами
Система прав доступа: модераторы и владельцы
Динамические сериализаторы для публичного/приватного просмотра
Фильтрация и сортировка платежей

### 📁 Структура проекта

├── config/              # Настройки Django
├── users/               # Приложение пользователей
├── courses/             # Приложение курсов
├── static/              # Статические файлы
├── media/               # Медиа файлы
├── requirements.txt     # Зависимости
├── manage.py           # Django CLI
└── README.md           # Документация

### 👥 Права доступа
Обычные пользователи: CRUD для своих курсов/уроков
Модераторы: Просмотр и редактирование всех курсов
Суперпользователи: Полный доступ через админку

### 🔧 Настройки
Проект использует python-decouple для управления настройками через .env файл:

env
DEBUG=True
SECRET_KEY=your-secret-key
DB_NAME=your_db
DB_USER=postgres
DB_PASSWORD=your_password

### Скрипт для инициализации проекта

**`setup_project.py`:**

#### setup_project.py
import os
import subprocess
import sys

def setup_project():
    print("=" * 60)
    print("НАСТРОЙКА ПРОЕКТА ОБРАЗОВАТЕЛЬНОЙ ПЛАТФОРМЫ")
    print("=" * 60)
    
    # 1. Проверка Python

    print("\n1. Проверка версии Python...")
    python_version = sys.version_info
    if python_version.major == 3 and python_version.minor >= 11:
        print(f"   ✓ Python {python_version.major}.{python_version.minor}.{python_version.micro}")
    else:
        print(f"   ✗ Требуется Python 3.11+, у вас {python_version.major}.{python_version.minor}")
        return
    
    # 2. Проверка виртуального окружения

    print("\n2. Проверка виртуального окружения...")
    if hasattr(sys, 'real_prefix') or (hasattr(sys, 'base_prefix') and sys.base_prefix != sys.prefix):
        print("   ✓ Виртуальное окружение активировано")
    else:
        print("   ⚠️  Виртуальное окружение не активировано")
        print("   Рекомендуется: python -m venv venv")
    
    # 3. Установка зависимостей

    print("\n3. Установка зависимостей...")
    try:
        subprocess.check_call([sys.executable, "-m", "pip", "install", "-r", "requirements.txt"])
        print("   ✓ Зависимости установлены")
    except:
        print("   ✗ Ошибка установки зависимостей")
        print("   Выполните вручную: pip install -r requirements.txt")
    
    # 4. Проверка .env файла

    print("\n4. Проверка настроек...")
    if os.path.exists('.env'):
        print("   ✓ Файл .env найден")
    else:
        print("   ⚠️  Файл .env не найден")
        print("   Создайте .env файл из .env.example")
    
    # 5. Инструкции

    print("\n" + "=" * 60)
    print("СЛЕДУЮЩИЕ ШАГИ:")
    print("=" * 60)
    print("1. Настройте базу данных в .env файле")
    print("2. Выполните миграции:")
    print("   python manage.py migrate")
    print("3. Создайте суперпользователя:")
    print("   python manage.py createsuperuser")
    print("4. Запустите сервер:")
    print("   python manage.py runserver")
    print("5. Протестируйте API:")
    print("   python test_final.py")
    print("\nГотово! 🎉")

if __name__ == "__main__":
    setup_project()

### 📄 Лицензия
MIT